import styled from 'styled-components';
import { Section as SectionOriginal, Row as RowOriginal } from '../../styles';

export const Section = styled(SectionOriginal)`
  background: #faf6fb;
`;

export const Row = styled(RowOriginal)`
  margin: 0;
`;
